<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Movie</title>
    <style>
        * { box-sizing: border-box; font-family: 'Inter', sans-serif; }
        body { margin:0; min-height:100vh; background:#ffe6f0; display:flex; justify-content:center; align-items:center; }

        .edit-container { width:100%; max-width:500px; background:#ffccdf; padding:40px; border-radius:20px; box-shadow:0 12px 25px rgba(179,0,89,0.3); }

        h1 { font-size:26px; text-align:center; margin-bottom:25px; color:#b30059; }

        label { display:block; font-size:14px; margin-bottom:6px; color:#330033; }
        input { width:100%; padding:12px; font-size:14px; border:none; background:#ffe6f0; margin-bottom:18px; border-radius:8px; transition:0.3s; }
        input:focus { outline:none; box-shadow:0 0 5px #b30059; }

        .actions { display:flex; gap:15px; margin-top:15px; }
        button, .cancel { flex:1; padding:12px 0; font-size:14px; font-weight:600; border-radius:8px; text-align:center; cursor:pointer; transition:0.3s; }
        button { background:#b30059; color:#fff; border:none; }
        button:hover { background:#ff3399; }
        .cancel { background:transparent; border:2px solid #b30059; color:#330033; }
        .cancel:hover { background:#b30059; color:#fff; }

    </style>
</head>

<body>

    <div class="edit-container">
        <h1>Edit Movie Details</h1>

        <form action="/edit/<%= movie._id %>" method="POST">
            <label>Movie Title</label>
            <input type="text" name="moviename" value="<%= movie.moviename %>" required placeholder="Enter movie title">

            <label>Author / Director</label>
            <input type="text" name="author" value="<%= movie.author %>" required placeholder="Enter author or director">

            <label>Price (₹)</label>
            <input type="number" name="price" value="<%= movie.price %>" required placeholder="Enter movie price">

            <label>Category / Genre</label>
            <input type="text" name="category" value="<%= movie.category %>" required placeholder="Action, Drama, Comedy...">

            <label>Image URL</label>
            <input type="text" name="img" value="<%= movie.img %>" required placeholder="Paste image URL">

            <div class="actions">
                <button type="submit">UPDATE MOVIE</button>
                <a href="/" class="cancel">CANCEL</a>
            </div>
        </form>
    </div>

</body>

</html>
